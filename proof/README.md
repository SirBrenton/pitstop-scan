# Proof Library

Small, shareable artifacts showing: receipts → hazards → guardrails.

- `demo_pack_v0/` — synthetic demo output (derived aggregates only)
- `svix-2200/` — Retry-After / 429 semantics case study (derived aggregates only)
- plano-429-failover — discussion of retry vs failover semantics for HTTP 429
